<Window x:Class="FrienddOrganizer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrienddOrganizer.UI"
        xmlns:view="clr-namespace:FrienddOrganizer.UI.View"
        mc:Ignorable="d"
        Title="FriendOrganizer" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.Children>

            <view:NavigationView Grid.Column="0" DataContext="{Binding NavigationViewModel,Mode=OneWay}"> </view:NavigationView>
            <view:FriendDetailViewModel  Grid.Column="1" DataContext="{Binding FriendDetailViewModel,Mode=OneWay}"></view:FriendDetailViewModel>
            
            
        </Grid.Children>
    </Grid>
</Window>
